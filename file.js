const fs = require("fs");

//fs.writeFile('./testasync.txt', "hey, there. this is async", (err) => {});

//fs.writeFileSync('./test.txt', "hey there");

//fs.readFileSync("./someFile.txt", "utf-8");

/*
fs.readFile("./someFile.txt", "utf-8", (err, result) => {
 if (err) { console.log(err);}
 else { console.log(result);}
});
*/

//fs.appendFileSync("./test.txt", `some new task\n`);

//fs.appendFile("./test.txt", `some new task\n`, (err, result) => {});

//fs.unlinkSync

//fs.statSync

//fs.cpSync

//fs.mkdir //recursive: true
